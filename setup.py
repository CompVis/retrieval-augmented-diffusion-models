from setuptools import setup, find_packages

setup(
    name='rdm',
    version='0.0.1',
    description='',
    packages=["rdm"],
    install_requires=[
        'torch',
        'numpy',
        'tqdm',
    ],
)
